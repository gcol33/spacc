% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extrapolate_area.R
\name{extrapolateArea}
\alias{extrapolateArea}
\title{Extrapolate Richness to a Larger Area (Ugland T-S Curve)}
\usage{
extrapolateArea(
  x,
  coords,
  target_area = NULL,
  n_subareas = 10L,
  model = c("michaelis-menten", "asymptotic", "weibull", "lomolino", "logistic"),
  n_combos = 30L,
  R = 200L,
  level = 0.95,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{x}{A site-by-species matrix (presence/absence or abundance; binarized).}

\item{coords}{A two-column matrix or data frame of site coordinates (x, y).}

\item{target_area}{Numeric. Area to extrapolate to, in the squared units of
\code{coords}. Defaults to twice the observed (convex-hull) area.}

\item{n_subareas}{Integer. Number of spatial subareas to partition sites into
(k-means on coordinates). Default 10; must be between 4 and the number of
sites.}

\item{model}{Character. Area model to fit: \code{"michaelis-menten"} (default),
\code{"asymptotic"}, \code{"weibull"}, \code{"lomolino"}, or \code{"logistic"}.}

\item{n_combos}{Integer. Number of random subarea combinations averaged per
T-S point (all combinations are used when there are fewer). Default 30.}

\item{R}{Integer. Number of site-bootstrap replicates for the confidence
interval. Default 200.}

\item{level}{Numeric. Confidence level. Default 0.95.}

\item{seed}{Optional integer for reproducibility.}

\item{progress}{Logical. Show a progress message. Default \code{TRUE}.}
}
\value{
An object of class \code{spacc_area} with components:
\item{ts_curve}{Data frame of the T-S curve (\code{n_sub}, \code{area}, \code{richness},
\code{richness_sd})}
\item{fit}{The fitted \code{nls} area model}
\item{observed_area, observed_richness}{Convex-hull area and richness of the
full sample}
\item{target_area}{Area extrapolated to}
\item{predicted_richness, predicted_ci}{Extrapolated richness and its
bootstrap interval}
\item{asymptote, asymptote_ci}{Model asymptote and its bootstrap interval}
}
\description{
Estimate species richness for a spatial extent larger than the one sampled,
using the total-species (T-S) curve of Ugland, Gray & Ellingsen (2003).
Sites are partitioned into spatial subareas; the T-S curve is the expected
total richness of random combinations of subareas plotted against their
(convex-hull) area; an asymptotic model is fitted to that curve and
extrapolated to a target area, with a site-bootstrap confidence interval.
}
\details{
Unlike \code{\link[=extrapolate]{extrapolate()}}, which extends the curve in \emph{sample count}, this
answers "how many species in a region of a given \emph{area}", including areas
beyond the sampled extent.
}
\examples{
\donttest{
coords <- data.frame(x = runif(150), y = runif(150))
species <- matrix(rbinom(150 * 60, 1, 0.15), nrow = 150)
area_fit <- extrapolateArea(species, coords, n_subareas = 8, progress = FALSE)
print(area_fit)
}

}
\references{
Ugland, K.I., Gray, J.S. & Ellingsen, K.E. (2003). The species-accumulation
curve and estimation of species richness. Journal of Animal Ecology, 72,
888-897. \doi{10.1046/j.1365-2656.2003.00748.x}
}
\seealso{
\code{\link[=extrapolate]{extrapolate()}} for sample-count extrapolation; \code{\link[=chao2]{chao2()}} for
nonparametric richness.
}
