% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evenness.R
\name{evenness}
\alias{evenness}
\title{Evenness Profiles}
\usage{
evenness(
  x,
  q = seq(0.1, 3, by = 0.1),
  type = c("hill", "pielou", "simpson"),
  coords = NULL
)
}
\arguments{
\item{x}{A site-by-species matrix (abundance data).}

\item{q}{Numeric vector. Orders of diversity for Hill evenness.
Default \code{seq(0.1, 3, by = 0.1)}. Note: q = 0 is excluded by default
because Hill evenness at q = 0 is trivially S/S = 1.}

\item{type}{Character. Evenness type: \code{"hill"} (Hill evenness E_q = D_q / D_0,
default), \code{"pielou"} (Pielou's J = log(D_1) / log(S)), or
\code{"simpson"} (Simpson evenness = (1/D_2) / S).}

\item{coords}{Optional data.frame with columns \code{x} and \code{y} for spatial
mapping. When provided, enables \code{plot(type = "map")}.}
}
\value{
An object of class \code{spacc_evenness} containing:
\item{per_site}{Matrix or vector of per-site evenness values}
\item{regional}{Regional (pooled) evenness}
\item{q}{Orders used (for Hill type)}
\item{type}{Evenness type}
\item{coords}{Coordinates if provided}
\item{n_sites}{Number of sites}
\item{n_species}{Number of species}
}
\description{
Compute species evenness across sites using Hill-based, Pielou, or
Simpson evenness measures.
}
\details{
All evenness measures are bounded in \eqn{[0, 1]}:
\itemize{
\item 0 = maximally uneven (one dominant species)
\item 1 = perfectly even (all species equally abundant)
}

\strong{Hill evenness} (Jost 2010):
\deqn{E_q = D_q / D_0}
This divides the effective number of species at order q by species richness.

\strong{Pielou's J} (Pielou 1966):
\deqn{J = \frac{\log(D_1)}{\log(S)} = \frac{H'}{\log(S)}}

\strong{Simpson evenness}:
\deqn{E_{1/D} = \frac{1}{D_2 \cdot S}}
}
\examples{
species <- matrix(rpois(50 * 30, 2), nrow = 50)

# Hill evenness profile
ev <- evenness(species)
print(ev)

# Pielou's J
ev_j <- evenness(species, type = "pielou")
print(ev_j)

}
\references{
Jost, L. (2010). The relation between evenness and diversity. Diversity, 2,
207-232.

Pielou, E.C. (1966). The measurement of diversity in different types of
biological collections. Journal of Theoretical Biology, 13, 131-144.
}
\seealso{
\code{\link[=diversityProfile]{diversityProfile()}} for Hill number profiles,
\code{\link[=alphaDiversity]{alphaDiversity()}} for raw diversity values
}
