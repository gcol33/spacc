% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hill.R
\name{spaccHillBeta}
\alias{spaccHillBeta}
\title{Spatial Hill Number Beta Diversity}
\usage{
spaccHillBeta(
  x,
  coords,
  q = c(0, 1, 2),
  n_seeds = 50L,
  distance = c("euclidean", "haversine"),
  parallel = TRUE,
  n_cores = NULL,
  progress = TRUE,
  seed = NULL
)
}
\arguments{
\item{x}{A site-by-species matrix (rows = sites, cols = species).}

\item{coords}{A data.frame with columns \code{x} and \code{y}, or a \code{spacc_dist} object.}

\item{q}{Numeric vector. Orders of diversity. Default \code{c(0, 1, 2)}.}

\item{n_seeds}{Integer. Number of random starting points. Default 50.}

\item{distance}{Character. \code{"euclidean"} or \code{"haversine"}.}

\item{parallel}{Logical. Use parallel processing? Default \code{TRUE}.}

\item{n_cores}{Integer. Number of cores. Default \code{NULL}.}

\item{progress}{Logical. Show progress? Default \code{TRUE}.}

\item{seed}{Integer. Random seed.}
}
\value{
An object of class \code{spacc_hill_beta} containing:
\item{gamma}{Named list of n_seeds x n_sites matrices (one per q)}
\item{alpha}{Named list of n_seeds x n_sites matrices (one per q)}
\item{beta}{Named list of n_seeds x n_sites matrices (one per q)}
\item{q}{Vector of q values}
\item{coords}{Original coordinates}
\item{n_seeds, n_sites, n_species}{Dimensions}
}
\description{
Compute multisite beta diversity as gamma/alpha decomposition of Hill
numbers along the spatial accumulation curve (Jost 2007 framework).
}
\details{
At each accumulation step k, the function computes:
\itemize{
\item \strong{Gamma}: Hill number of the pooled community (all k sites combined)
\item \strong{Alpha}: Generalized mean of per-site Hill numbers (Jost's power mean)
\item \strong{Beta}: gamma / alpha (effective number of distinct communities)
}

Beta = 1 means all sites are identical; beta = k means all sites are
completely different. This provides the Hill-number analogue of the
Baselga-based \code{spaccBeta()}.
}
\examples{
\donttest{
coords <- data.frame(x = runif(40), y = runif(40))
species <- matrix(rpois(40 * 20, 2), nrow = 40)

hb <- spaccHillBeta(species, coords, n_seeds = 10, progress = FALSE)
plot(hb)
}

}
\references{
Jost, L. (2007). Partitioning diversity into independent alpha and beta
components. Ecology, 88, 2427-2439.
}
\seealso{
\code{\link[=spaccBeta]{spaccBeta()}} for P/A-based Baselga partitioning,
\code{\link[=spaccHill]{spaccHill()}} for Hill accumulation without beta decomposition
}
