% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_decay.R
\name{betaDecay}
\alias{betaDecay}
\title{Beta Distance-Decay}
\usage{
betaDecay(
  x,
  coords,
  index = c("sorensen", "jaccard"),
  model = c("all", "exponential", "power", "linear"),
  distance = c("euclidean", "haversine"),
  n_bins = 50L,
  progress = TRUE
)
}
\arguments{
\item{x}{A site-by-species matrix (presence/absence or abundance), or a
\code{spacc} object (will use its stored coordinates).}

\item{coords}{A data.frame with columns \code{x} and \code{y}, or a \code{spacc_dist}
object.}

\item{index}{Character. Dissimilarity index: \code{"sorensen"} (default) or
\code{"jaccard"}.}

\item{model}{Character. Decay model to fit: \code{"exponential"}, \code{"power"},
\code{"linear"}, or \code{"all"} (default). When \code{"all"}, fits all three and selects
the best by AIC.}

\item{distance}{Character. Distance method: \code{"euclidean"} (default) or
\code{"haversine"}.}

\item{n_bins}{Integer. Number of distance bins for binned means in plots.
Default 50.}

\item{progress}{Logical. Show progress messages? Default \code{TRUE}.}
}
\value{
An object of class \code{spacc_beta_decay} containing:
\item{pairs}{Data.frame with columns \code{distance}, \code{dissimilarity}, \code{site_i},
\code{site_j}}
\item{fits}{Named list of fitted model objects}
\item{best_model}{Name of best model by AIC}
\item{half_life}{Distance at which similarity halves (exponential model only)}
\item{coefficients}{Data.frame of model coefficients and AIC}
\item{index}{Dissimilarity index used}
\item{n_sites}{Number of sites}
\item{n_pairs}{Number of pairwise comparisons}
}
\description{
Compute the distance-decay of community similarity by fitting models to
pairwise beta dissimilarity as a function of geographic distance.
}
\details{
Three decay models are available:
\itemize{
\item \strong{Exponential}: \eqn{\beta = 1 - a \cdot e^{-b \cdot d}}
\item \strong{Power}: \eqn{\beta = a \cdot d^b}
\item \strong{Linear}: \eqn{\beta = a + b \cdot d}
}

The half-life (exponential model) is the distance at which similarity
decays to half its initial value: \eqn{d_{1/2} = \ln(2) / b}.
}
\examples{
\donttest{
coords <- data.frame(x = runif(30), y = runif(30))
species <- matrix(rbinom(30 * 20, 1, 0.3), nrow = 30)

bd <- betaDecay(species, coords)
print(bd)
plot(bd)
}

}
\references{
Nekola, J.C. & White, P.S. (1999). The distance decay of similarity in
biogeography and ecology. Journal of Biogeography, 26, 867-878.

Morlon, H., Chuyong, G., Condit, R., et al. (2008). A general framework
for the distance-decay of similarity in ecological communities. Ecology
Letters, 11, 904-917.
}
\seealso{
\code{\link[=spaccBeta]{spaccBeta()}} for spatial beta diversity accumulation,
\code{\link[=distanceDecay]{distanceDecay()}} for species similarity decay
}
