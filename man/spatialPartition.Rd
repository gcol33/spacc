% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mem.R
\name{spatialPartition}
\alias{spatialPartition}
\title{Spatial Variance Partitioning with MEMs}
\usage{
spatialPartition(x, mem, metric = NULL, forward = TRUE, alpha = 0.05)
}
\arguments{
\item{x}{A spacc result object (e.g., \code{spacc_metrics}, \code{spacc_alpha},
\code{spacc_hill}) or a numeric vector of per-site diversity values.}

\item{mem}{A \code{spacc_mem} object from \code{\link[=spatialEigenvectors]{spatialEigenvectors()}}.}

\item{metric}{Character. Which metric to extract from \code{x} (passed to
internal extraction). Default \code{NULL} uses the first available.}

\item{forward}{Logical. Use forward selection? Default \code{TRUE}.
If \code{FALSE}, all MEMs are included.}

\item{alpha}{Numeric. Significance threshold for forward selection.
Default 0.05.}
}
\value{
An object of class \code{spacc_mem_partition} containing:
\item{r_squared_spatial}{R-squared of the spatial model}
\item{r_squared_total}{Total R-squared (same as spatial here)}
\item{selected_mems}{Names of selected MEM vectors}
\item{n_selected}{Number of selected MEMs}
\item{anova_table}{ANOVA table from the final model}
\item{coefficients}{Model coefficients}
}
\description{
Partition diversity variation into spatial and non-spatial components
using forward selection of Moran's Eigenvector Maps.
}
\details{
Forward selection of MEMs proceeds by adding the MEM that most improves
the model AIC at each step, stopping when no MEM improves AIC by more
than 2 units or when p > alpha.
}
\examples{
\donttest{
coords <- data.frame(x = runif(30), y = runif(30))
species <- matrix(rpois(30 * 15, 2), nrow = 30)

mem <- spatialEigenvectors(coords)
alpha <- alphaDiversity(species, q = 0)
part <- spatialPartition(alpha$q0, mem)
print(part)
}

}
\seealso{
\code{\link[=spatialEigenvectors]{spatialEigenvectors()}} for computing MEMs
}
