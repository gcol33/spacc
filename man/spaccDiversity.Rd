% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom.R
\name{spaccDiversity}
\alias{spaccDiversity}
\title{Spatial Accumulation of a Custom Diversity Metric}
\usage{
spaccDiversity(
  x,
  coords,
  fun,
  ...,
  method = c("knn", "kncn", "random", "radius", "collector"),
  incidence = FALSE,
  n_seeds = 50L,
  distance = c("euclidean", "haversine"),
  progress = TRUE,
  seed = NULL
)
}
\arguments{
\item{x}{A site-by-species matrix (rows = sites, cols = species), abundance
or presence/absence.}

\item{coords}{A data.frame with columns \code{x} and \code{y}, or a \code{spacc_dist} object.}

\item{fun}{A function applied to the cumulative community at each step. It
receives a named numeric vector of length \code{ncol(x)} (cumulative summed
abundances, or 0/1 incidences when \code{incidence = TRUE}) plus any arguments
passed through \code{...}, and must return a single numeric value.}

\item{...}{Additional arguments passed to \code{fun}.}

\item{method}{Character. Spatial ordering of sites: \code{"knn"} (default),
\code{"kncn"}, \code{"random"}, \code{"radius"}, or \code{"collector"}.}

\item{incidence}{Logical. If \code{TRUE}, \code{fun} receives 0/1 incidences instead
of summed abundances. Default \code{FALSE}.}

\item{n_seeds}{Integer. Number of random starting points / orderings.
Ignored for \code{"collector"} (a single data-order curve). Default 50.}

\item{distance}{Character. \code{"euclidean"} or \code{"haversine"}.}

\item{progress}{Logical. Show progress? Default \code{TRUE}.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
An object of class \code{spacc_diversity} that inherits from \code{spacc}, so
the standard \code{summary()}, \code{plot()}, \code{as.data.frame()} and \code{predict()}
methods apply. \code{curves} is an \verb{n_seeds x n_sites} matrix of the metric
along the accumulation.
}
\description{
Accumulate any user-supplied diversity index along a spatial ordering of
sites. At each accumulation step the cumulative community is passed to
\code{fun}, which returns a single number. This is the general escape hatch
behind the built-in metric functions: use it for indices that spacc does
not implement directly.
}
\details{
The site ordering reuses the same spatial traversals as the built-in
methods (nearest-neighbour, nearest-centroid, random, distance-rank, or
data order), then evaluates \code{fun} on the accumulating community. Because the
index is an arbitrary R function, this trades the speed of the compiled
metrics for full flexibility.
}
\examples{
\donttest{
coords <- data.frame(x = runif(40), y = runif(40))
species <- matrix(rpois(40 * 20, 2), nrow = 40)

# Shannon entropy along the accumulation
shannon <- function(comm) {
  p <- comm[comm > 0] / sum(comm)
  -sum(p * log(p))
}
div <- spaccDiversity(species, coords, shannon, n_seeds = 20)
plot(div)
}

}
\seealso{
\code{\link[=spaccHill]{spaccHill()}}, \code{\link[=spaccPhylo]{spaccPhylo()}}, \code{\link[=spaccFunc]{spaccFunc()}} for built-in metrics.
}
