% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zeta.R
\name{zetaDiversity}
\alias{zetaDiversity}
\title{Zeta Diversity}
\usage{
zetaDiversity(
  x,
  coords,
  orders = 1:10,
  n_samples = 100L,
  method = c("knn", "random"),
  distance = c("euclidean", "haversine"),
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{x}{A site-by-species matrix (presence/absence or abundance).
Automatically binarized.}

\item{coords}{A data.frame with columns \code{x} and \code{y}, or a \code{spacc_dist} object.}

\item{orders}{Integer vector. Orders of zeta diversity to compute (number of
sites in each combination). Default \code{1:10}.}

\item{n_samples}{Integer. Number of random combinations to sample per order.
Default 100.}

\item{method}{Character. Method for selecting k-site combinations:
\code{"knn"} (spatially nearest sites) or \code{"random"} (random combinations).
Default \code{"knn"}.}

\item{distance}{Character. Distance method: \code{"euclidean"} or \code{"haversine"}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{NULL}.}

\item{progress}{Logical. Show progress? Default \code{TRUE}.}
}
\value{
An object of class \code{spacc_zeta} containing:
\item{zeta}{Mean zeta values per order}
\item{zeta_sd}{Standard deviations per order}
\item{orders}{The k values}
\item{n_samples}{Number of samples per order}
\item{ratio}{Zeta ratio: zeta_k / zeta_(k-1)}
\item{decline}{Data.frame with exponential and power-law fit statistics}
\item{method}{Method used}
\item{n_sites}{Number of sites}
\item{n_species}{Total species count}
}
\description{
Compute zeta diversity — the mean number of species shared across k sites —
for increasing orders of k. The zeta decline curve reveals community assembly
processes: exponential decline suggests stochastic assembly, while power-law
decline indicates niche-based assembly.
}
\details{
Zeta diversity of order k (\eqn{\zeta_k}) is the mean number of species
shared across k sites. Key properties:
\itemize{
\item \eqn{\zeta_1} = mean species richness per site
\item \eqn{\zeta_2} = mean number of species shared by any two sites
\item \eqn{\zeta_k} decreases monotonically with k
}

The zeta decline ratio (\eqn{\zeta_k / \zeta_{k-1}}) is diagnostic:
\itemize{
\item Constant ratio: exponential decline (stochastic assembly)
\item Increasing ratio: power-law decline (deterministic/niche-based assembly)
}

The \code{knn} method selects spatially nearest k sites from each focal site,
which is ecologically meaningful for testing spatial turnover. The \code{random}
method samples random k-site combinations, providing a null expectation.
}
\examples{
\donttest{
coords <- data.frame(x = runif(30), y = runif(30))
species <- matrix(rbinom(30 * 20, 1, 0.3), nrow = 30)
zeta <- zetaDiversity(species, coords, orders = 1:5, n_samples = 50)
plot(zeta)
}

}
\references{
Hui, C. & McGeoch, M.A. (2014). Zeta diversity as a concept and metric
that unifies incidence-based biodiversity patterns. The American Naturalist,
184, 684-694.

Latombe, G., McGeoch, M.A., Nipperess, D.A. & Hui, C. (2018). zetadiv:
an R package for computing compositional change across multiple sites,
assemblages or cases. bioRxiv, 324897.
}
\seealso{
\code{\link[=spaccBeta]{spaccBeta()}} for pairwise beta diversity, \code{\link[=distanceDecay]{distanceDecay()}} for
distance-decay relationships
}
