% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity.R
\name{diversityProfileFunc}
\alias{diversityProfileFunc}
\title{Functional Diversity Profile}
\usage{
diversityProfileFunc(
  x,
  traits,
  q = seq(0, 3, by = 0.1),
  type = c("both", "per_site", "regional"),
  dist_method = c("euclidean", "gower"),
  normalize = TRUE,
  coords = NULL
)
}
\arguments{
\item{x}{A site-by-species matrix (abundance data). Column names must match
row names in \code{traits}.}

\item{traits}{A data.frame of species traits. Row names must match column
names in \code{x}.}

\item{q}{Numeric vector. Orders of diversity. Default \code{seq(0, 3, by = 0.1)}.}

\item{type}{Character. What to compute: \code{"per_site"}, \code{"regional"}, or
\code{"both"} (default).}

\item{dist_method}{Character. Distance method for trait matrix:
\code{"euclidean"} (default) or \code{"gower"}.}

\item{normalize}{Logical. Normalize distances to [0, 1]? Default \code{TRUE}.}

\item{coords}{Optional data.frame with \code{x} and \code{y} for spatial mapping.}
}
\value{
An object of class \code{spacc_profile} with \verb{$profile_type = "functional"}.
}
\description{
Compute functional Hill numbers (Leinster & Cobbold 2012) across a continuous
range of diversity orders (q), producing a functional diversity profile based
on trait similarity.
}
\details{
Functional Hill numbers (Leinster & Cobbold 2012) incorporate trait similarity
via a similarity matrix Z = 1 - D. When all species are maximally dissimilar
(Z = identity), this reduces to standard Hill numbers.
}
\examples{
\donttest{
species <- matrix(rpois(20 * 10, 2), nrow = 20,
                  dimnames = list(NULL, paste0("sp", 1:10)))
traits <- data.frame(
  body_size = rnorm(10), diet = rnorm(10),
  row.names = paste0("sp", 1:10)
)
prof <- diversityProfileFunc(species, traits)
print(prof)
}

}
\references{
Leinster, T. & Cobbold, C.A. (2012). Measuring diversity: the importance
of species similarity. Ecology, 93, 477-489.
}
\seealso{
\code{\link[=diversityProfile]{diversityProfile()}} for taxonomic profiles,
\code{\link[=diversityProfilePhylo]{diversityProfilePhylo()}} for phylogenetic profiles
}
