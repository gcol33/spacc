% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hill.R
\name{spaccHill}
\alias{spaccHill}
\title{Spatial Accumulation with Hill Numbers}
\usage{
spaccHill(
  x,
  coords,
  q = c(0, 1, 2),
  n_seeds = 50L,
  method = "knn",
  distance = c("euclidean", "haversine"),
  parallel = TRUE,
  n_cores = NULL,
  progress = TRUE,
  seed = NULL,
  map = FALSE
)
}
\arguments{
\item{x}{A site-by-species matrix (rows = sites, cols = species) with
presence/absence (0/1) or abundance data.}

\item{coords}{A data.frame with columns \code{x} and \code{y} containing site coordinates,
or a \code{spacc_dist} object from \code{\link[=distances]{distances()}}.}

\item{q}{Numeric vector. Orders of diversity to compute. Default \code{c(0, 1, 2)}.
\itemize{
\item q = 0: Species richness
\item q = 1: Exponential of Shannon entropy (effective common species)
\item q = 2: Inverse Simpson (effective dominant species)
}}

\item{n_seeds}{Integer. Number of random starting points. Default 50.}

\item{method}{Character. Accumulation method: \code{"knn"} (default).}

\item{distance}{Character. Distance method: \code{"euclidean"} or \code{"haversine"}.}

\item{parallel}{Logical. Use parallel processing? Default \code{TRUE}.}

\item{n_cores}{Integer. Number of cores. Default \code{NULL} uses \code{detectCores() - 1}.}

\item{progress}{Logical. Show progress bar? Default \code{TRUE}.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{map}{Logical. If \code{TRUE}, run accumulation from every site as seed
and store per-site final Hill numbers for spatial mapping. Enables
\code{\link[=as_sf]{as_sf()}} and \code{plot(type = "map")}. Default \code{FALSE}.}
}
\value{
An object of class \code{spacc_hill} containing:
\item{curves}{Named list of matrices, one per q value (n_seeds x n_sites)}
\item{q}{Vector of q values used}
\item{coords}{Original coordinates}
\item{n_seeds}{Number of seeds}
\item{n_sites}{Number of sites}
\item{n_species}{Total species}
\item{method}{Method used}
}
\description{
Compute spatial species accumulation curves using Hill numbers (effective
number of species) instead of raw richness. Hill numbers unify diversity
measures: q=0 is richness, q=1 is exponential Shannon, q=2 is inverse Simpson.
}
\details{
Hill numbers (Chao et al. 2014) provide a unified framework for diversity
measurement. Unlike raw richness (q=0), higher-order Hill numbers (q=1, q=2)
down-weight rare species, providing different perspectives on diversity.

The spatial accumulation of Hill numbers can reveal scale-dependent diversity
patterns missed by richness alone.
}
\examples{
\dontrun{
# Compare diversity at different orders
coords <- data.frame(x = runif(50), y = runif(50))
species <- matrix(rpois(50 * 30, 2), nrow = 50)

hill <- spaccHill(species, coords, q = c(0, 1, 2))
plot(hill)

# Extract summary at final site
summary(hill)
}

}
\references{
Chao, A., Gotelli, N.J., Hsieh, T.C., Sander, E.L., Ma, K.H., Colwell, R.K.
& Ellison, A.M. (2014). Rarefaction and extrapolation with Hill numbers:
a framework for sampling and estimation in species diversity studies.
Ecological Monographs, 84, 45-67.
}
\seealso{
\code{\link[=spacc]{spacc()}} for richness-only accumulation, \code{\link[iNEXT:iNEXT]{iNEXT::iNEXT()}} for
non-spatial Hill number rarefaction
}
