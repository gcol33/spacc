% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.spacc}
\alias{predict.spacc}
\title{Predict from Empirical Accumulation Curve}
\usage{
\method{predict}{spacc}(object, n = NULL, ci = TRUE, ci_level = 0.95, warn = TRUE, ...)
}
\arguments{
\item{object}{A \code{spacc} object.}

\item{n}{Numeric vector of site counts at which to interpolate. Defaults to
25\\%, 50\\%, and 100\\% of total sites.}

\item{ci}{Logical. If \code{TRUE} (default), return a data frame with columns
\code{n}, \code{mean}, \code{lower}, \code{upper}. If \code{FALSE}, return a named numeric vector.}

\item{ci_level}{Confidence level for the interval (default 0.95).}

\item{warn}{Logical. Warn when \code{n} values fall outside the observed range
(default \code{TRUE}).}

\item{...}{Ignored.}
}
\value{
A data frame (if \code{ci = TRUE}) or named numeric vector (if \code{ci = FALSE}).
Out-of-range values return \code{NA}.
}
\description{
Interpolate the mean empirical accumulation curve at arbitrary site counts
using linear interpolation. Unlike the predict method for \code{spacc_fit}
objects, this does not use a fitted model; it interpolates the observed
curve directly.
}
\examples{
\donttest{
coords <- data.frame(x = runif(50), y = runif(50))
species <- matrix(rbinom(50 * 30, 1, 0.3), nrow = 50)
sac <- spacc(species, coords, n_seeds = 10, progress = FALSE)
predict(sac, n = c(10, 25, 50))
predict(sac, n = c(10, 25), ci = FALSE)
}

}
