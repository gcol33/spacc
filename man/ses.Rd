% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ses.R
\name{ses}
\alias{ses}
\title{Standardized Effect Size (SES) via Null Models}
\usage{
ses(
  x,
  species,
  coords = NULL,
  metric = NULL,
  null_model = c("frequency", "richness", "both", "curveball"),
  n_perm = 999L,
  parallel = TRUE,
  n_cores = NULL,
  progress = TRUE,
  seed = NULL
)
}
\arguments{
\item{x}{A spacc output object. Supported classes: \code{spacc}, \code{spacc_hill},
\code{spacc_phylo}, \code{spacc_func}, \code{spacc_beta}, \code{spacc_metrics},
\code{spacc_alpha}, \code{spacc_partition}.}

\item{species}{A site-by-species matrix (required). The species matrix used
to produce \code{x}. Needed because spacc objects do not store the raw species
matrix.}

\item{coords}{Optional data.frame with columns \code{x} and \code{y}. Required if the
original analysis used coordinates. If \code{x} contains stored coordinates,
they will be used automatically.}

\item{metric}{Character or \code{NULL}. For multi-metric objects (e.g.,
\code{spacc_hill} with multiple q), specify which metric to extract. If \code{NULL},
uses the first/default metric.}

\item{null_model}{Character. Null model algorithm:
\itemize{
\item \code{"frequency"}: Shuffle species columns independently (maintains column
totals = species frequency)
\item \code{"richness"}: Shuffle species rows independently (maintains row totals
= site richness)
\item \code{"both"}: Independent swap algorithm maintaining both row and column
totals (Gotelli 2000)
\item \code{"curveball"}: Curveball algorithm for efficient swap (Strona et al. 2014)
}}

\item{n_perm}{Integer. Number of permutations. Default 999.}

\item{parallel}{Logical. Use parallel processing for the underlying analysis?
Default \code{TRUE}.}

\item{n_cores}{Integer. Number of cores. Default \code{NULL} (auto-detect).}

\item{progress}{Logical. Show progress? Default \code{TRUE}.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
An object of class \code{spacc_ses} containing:
\item{observed}{Numeric vector of observed metric values}
\item{null_mean}{Mean of null distribution}
\item{null_sd}{Standard deviation of null distribution}
\item{ses}{Standardized effect size: (observed - null_mean) / null_sd}
\item{p_value}{Two-tailed p-value}
\item{n_perm}{Number of permutations}
\item{null_model}{Null model algorithm used}
\item{metric}{Metric name}
\item{input_class}{Class of input object}
}
\description{
Compute standardized effect sizes by comparing observed diversity metrics
against a null distribution from randomized communities. Supports multiple
null model algorithms and works with most spacc output classes.
}
\details{
SES is computed as:
\deqn{SES = \frac{observed - \bar{null}}{sd_{null}}}

A two-tailed p-value is calculated as the proportion of null values at
least as extreme as the observed value:
\deqn{p = \frac{2 \cdot \min(r, n_{perm} + 1 - r)}{n_{perm} + 1}}
where \eqn{r} is the rank of the observed value among null values.

\strong{Null model algorithms:}
\itemize{
\item \code{"frequency"}: Tests whether species composition matters given observed
species frequencies
\item \code{"richness"}: Tests whether species identity matters given observed site
richness
\item \code{"both"}: Maintains both marginal totals; tests non-random species
co-occurrence patterns
\item \code{"curveball"}: Efficient alternative to \code{"both"} with proven uniform
sampling properties
}
}
\examples{
\donttest{
coords <- data.frame(x = runif(20), y = runif(20))
species <- matrix(rbinom(20 * 15, 1, 0.3), nrow = 20)

sac <- spacc(species, coords, n_seeds = 10)
result <- ses(sac, species, n_perm = 19)
print(result)
}

}
\references{
Gotelli, N.J. (2000). Null model analysis of species co-occurrence patterns.
Ecology, 81, 2606-2621.

Strona, G., Nappo, D., Boccacci, F., Fattorini, S. & San-Miguel-Ayanz, J.
(2014). A fast and unbiased procedure to randomize ecological binary
matrices with fixed row and column totals. Nature Communications, 5, 4114.
}
\seealso{
\code{\link[=spaccHill]{spaccHill()}}, \code{\link[=spaccBeta]{spaccBeta()}}, \code{\link[=spaccMetrics]{spaccMetrics()}}
}
