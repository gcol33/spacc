% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{as_sf}
\alias{as_sf}
\title{Convert spacc_metrics to sf}
\usage{
as_sf(x, crs = NULL)
}
\arguments{
\item{x}{A \code{spacc_metrics} object.}

\item{crs}{Coordinate reference system. Default \code{NULL} (no CRS).
Use EPSG codes like \code{4326} for WGS84 or \code{32631} for UTM zone 31N.}
}
\value{
An sf object with POINT geometries and metric columns.
}
\description{
Convert metrics to an sf object for spatial analysis and integration
with the areaOfEffect package.
}
\examples{
\donttest{
coords <- data.frame(x = runif(50), y = runif(50))
species <- matrix(rbinom(50 * 30, 1, 0.3), nrow = 50)
metrics <- spaccMetrics(species, coords)
if (requireNamespace("sf", quietly = TRUE)) {
  metrics_sf <- as_sf(metrics)
}
}

}
