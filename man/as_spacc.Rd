% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_spacc.R
\name{as_spacc}
\alias{as_spacc}
\alias{as_spacc.default}
\alias{as_spacc.specaccum}
\title{Convert an Accumulation Curve from Another Package to a spacc Object}
\usage{
as_spacc(x, ...)

\method{as_spacc}{default}(x, ...)

\method{as_spacc}{specaccum}(x, ...)
}
\arguments{
\item{x}{An object to convert. A \code{specaccum} object from
\code{vegan::specaccum()} is supported.}

\item{...}{Ignored, for method extensibility.}
}
\value{
An object of class \code{spacc} with \code{print()}, \code{summary()}, \code{plot()},
\code{as.data.frame()}, and \code{\link[=compare]{compare()}} support.
}
\description{
Bring an existing species accumulation curve into the spacc object system so
it can be summarised, plotted, and compared with \code{\link[=compare]{compare()}} alongside
spatial curves. A \pkg{vegan} \code{specaccum} object is currently supported.
}
\details{
A \code{specaccum} object built with \code{method = "random"} stores one richness curve
per permutation in its \code{perm} matrix. Those curves become the rows of the
returned \code{curves} matrix and behave exactly like spacc seeds, so the
across-curve confidence band and the \code{\link[=compare]{compare()}} permutation, bootstrap, and
AUC tests carry over unchanged. A \code{specaccum} object without a permutation
matrix (for example \code{method = "exact"} or \code{"collector"}) contributes its
single mean curve.

Coordinates cannot be recovered from a \code{specaccum} object, so the result
carries no \code{coords}: it is a curve container for plotting and comparison, not
a re-runnable spatial object.
}
\examples{
\donttest{
if (requireNamespace("vegan", quietly = TRUE)) {
  data(BCI, package = "vegan")
  classic <- vegan::specaccum(BCI, method = "random")
  sp <- as_spacc(classic)
  summary(sp)
  plot(sp)
}
}

}
\seealso{
\code{\link[=compare]{compare()}} to test a converted curve against a spatial one.
}
