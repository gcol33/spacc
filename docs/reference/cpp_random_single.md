# Single Random Accumulation Curve

Visit sites in random order (null model for comparison).

## Usage

``` r
cpp_random_single(species_pa, order)
```

## Arguments

- species_pa:

  Integer matrix (sites x species), presence/absence

- order:

  Integer vector giving the order to visit sites (0-based)

## Value

Integer vector of cumulative species counts
